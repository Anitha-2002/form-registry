# Document uploads were moved to apps.signage.models.Document.
# This app is no longer in INSTALLED_APPS; keep the folder only if you still
# need to delete it manually. Do not re-enable "apps.documents" — it would
# duplicate the Requirement ↔ Document relationship.
